# 原神角色数据与首版实验
2026-09-22 更正：完整下载 .txt 后核实绫华117、钟离270、胡桃141，共528文本/526不同内容。下文历史529/527计入了绫华目录的 sortTXT.ipynb，不能继续作为文本容量。最新实现、开发快照和校准计划见 [GENSHIN_STAGE_B.md](GENSHIN_STAGE_B.md)；仍未完成语义分组及正式训练冻结。

日期：2026-09-21。历史选型记录，保留当时的容量核查和来源问题。当前范围与实施顺序统一见 [DESIGN.md](DESIGN.md)，本文件不再维护规模目标和实施计划。最新数据判断见 [来源审核报告](GENSHIN_STAGE_B_DATA_AUDIT.md)。

## 数据实际有多少

来源固定为 ChatHaruhi revision `290bf4ad22076156083804013012847a77c0646c`。完整 Git tree 未截断；下表文本数量来自目录和 Git blob 哈希，未下载并逐条阅读全部文本。

| 角色 | texts 文件数 | 字节完全去重后 | 生成对话行数 |
| --- | --- | --- | --- |
| 神里绫华 | 118 | 116 | 198 |
| 钟离 | 270 | 270 | 73 |
| 胡桃 | 141 | 141 | 76 |
| 合计 | 529 | 527（跨三角色亦为此数） | 347 |

绫华重复组是 `24.txt/25.txt`、`43.txt/44.txt`。三个生成文件位于 `kyon_generator/organized_data/{ayaka,zhongli,hutao}_test_output_dialogue.jsonl`，全部行均标 `source=synthesized`，字段只有 dialogue/source；文件名含 test 不等于有独立测试划分。绫华 `characters/ayaka/dialogues/test.jsonl` 实际为零字节，其余所查材料也未提供可直接沿用的场景划分。

本轮每角色按路径排序抽查首、中、末各一段 texts，读取三个 system_prompt 和三个完整生成文件，下载内容均与固定 tree 的 Git blob 哈希匹配。可复查例子：

- 绫华 `texts/1.txt` 是托马介绍其身份，没有绫华回答；`63.txt` 的旁白先叙述舞毕，再列舞前请求，不能按文件顺序直接构造时间历史；`阳光很好….txt` 是短语音情境。
- 钟离 `texts/——略懂门道。.txt` 有封印调查及下一步建议；`面对无法掌控的境遇时，人们总是喟叹自己的无力。.txt` 有神之眼和眼狩令相关知识，需约束剧情时期。system_prompt 含身份、喜好及大量外貌描述，但没有逐项来源指针。
- 胡桃 `texts/嗯，就这样吧，交给你了，就按这个写。.txt` 有宣传方式与职业惯例的分歧；`——不过这也说明，他们身上多半真有什么隐秘。.txt` 缺前文指代；system_prompt 描述平日活泼与葬礼肃穆的区别。生成文件首条却将神之眼说成能洞察一切、看透秘密，与上述钟离台本的元素力说明不一致，不能作为未经审查的正确答案。

## 信息与标签是否够

| 训练所需内容 | 当前证据 | 最小补充 |
| --- | --- | --- |
| 角色身份与行为依据 | 有角色提示和台本；强弱不一 | 精简外貌和模仿指令，给稳定偏好、职责、关系逐项绑定证据 |
| 当前情境、历史和玩家输入 | 部分多轮台本具备；另有语音、介绍和指代不明片段 | 恢复可知的时间顺序及响应截点；无法还原的片段只作背景或排除 |
| 原剧情定位与独立划分 | 有仓库路径，没有统一任务/场景 ID | 合并相邻剧情、重复和改写，建立 train/dev/test 场景族；不按文件随机切分 |
| DPO 偏好 | 无 chosen/rejected 或可靠评分 | 从同一输入采样候选，用校准后的裁判及证据形成偏好 |
| GRPO 奖励 | 无可直接使用的数值奖励 | 对当前策略组内回答评分；处理同分、无法判断和调用失败 |
| 工具、任务状态和长期记忆 | 这些文件没有结构化执行轨迹或相应评测标签 | 本阶段只评价给定历史与角色证据下的回答/行动，不宣称工具或长期记忆能力已验证 |

**判断：材料足以开始有限适配和角色行为优化试验，但尚不能证明满足正式实验规模。** 527 是不同文件内容数，不是独立事件数；347 条生成回答也不能加到独立情境数中。GRPO 可不依赖预先标注的正确答案或偏好，但必须有可信奖励，不能省略证据清理与裁判校准。

初期容量目标已撤销。现有来源是否支持独立评测以逐项审核为准，不以三角色覆盖、文件数或指定配额作为继续开发的条件；未验收材料仍不能进入正式训练。

## 来源与复查

- [固定上游目录](https://github.com/LC1332/Chat-Haruhi-Suzumiya/tree/290bf4ad22076156083804013012847a77c0646c/characters)。数据声明为 CC BY-NC 4.0，代码为 Apache-2.0，底层作品权利独立；不将其作为可任意商用或再分发的数据。
- 本地来源清单及下载：`data/raw/genshin_selection_20260921/`，含 tree、样例与 `sources.jsonl`；运行脚本与 `inventory.json`、`samples.json` 在 `runs/genshin_selection_20260921/`。均被 Git 忽略。
- 复查命令：`python runs/genshin_selection_20260921/inspect_genshin.py --samples`。本轮未调用模型、训练、租赁 GPU、外部上传或产生付费 API 成本。
