# 项目进度与交接

更新时间：2026-09-22。当前范围以 [DESIGN.md](DESIGN.md) 为准。

## 当前阶段

**最小 Agent 离线闭环可运行；GRPO 已接入成熟框架并完成微型更新验证。** 原创角色“岚”的档案递送是设计测试环境，不是原神剧情或盲测。已有真实基座任务失败轨迹；本轮新增裁判执行/统计、奖励处理和 TRL/PEFT 单步动作训练入口。真实裁判校准未通过，未训练Qwen或启动真实任务优化。

| 项目 | 实际状态 |
| --- | --- |
| 软件基础 | 已有 DPO、数据契约、无标签开发快照、离线裁判请求/解析、匿名校准包与来源审核；新增短任务工具闭环、CLI、事件记忆和可校验回放 |
| 原神数据 | 528文本/526份不同字节内容；313文本归9链/8保守块，不代表独立事件数；20项证据审核记录，未导出已验收profile |
| 已暴露开发材料 | 12个dev截点；同事件变体不能改作盲测 |
| 储备与划分 | 钟离60文件/2块、6个待审截点；正式train/test仍0/0，无正式冻结 |
| 裁判校准 | 原36对/72请求未改；本轮试跑2请求共4次本地推理，均严格解析失败，70请求未调用；人工0，calibration_passed=false |
| 模型与资源 | 已复用本地Qwen3-1.7B及现有Python 3.12/Transformers环境，在RTX 3060 Laptop 6 GB上完成BF16推理；裁判提供方/模型/价格仍未定 |
| 训练与效果 | 已有DPO；新增TRL 0.26.2 GRPO单步动作适配，CPU随机微型模型2步更新通过；无真实任务训练、完整多轮GRPO或效果改善结论 |

## 下一步最小任务

1. 从已保留的真实失败出发，诊断重复承诺与无进展动作；后续每次改动单独记录版本，不能覆盖本次失败或用脚本补模型动作。
2. 固定开发任务集合、模型和生成预算，做有/无事件记忆的单因素消融并报告原始计数；当前完整可见状态也包含承诺，移除事件记忆不等于移除所有历史信息。正式泛化比较仍需独立来源分组和未参与开发的留出，旧dev和本设计任务不能改称盲测。
3. 完成真实人工独立标注并选定能满足协议的裁判；现有1.7B模型试跑缺顶层reason，不能自动补齐当有效结果。原36对不扩充、不代填。原创任务仍需本任务校准与奖励验收；已有GRPO入口可先做preflight，通过后再开展明确授权的真实任务训练，复用DPO补对照。

## 具体阻塞

- **离线闭环：** 已交付，无运行阻塞；纯脚本与回放仅依赖标准库。
- **基座比较：** 本地模型与推理接入已验证，单次失败只支持开发诊断；还缺固定对照集合、多次轨迹、记忆消融、可比预算及独立留出。没有DPO/GRPO行为比较结果。
- **正式基座角色评测：** 原神输入证据与独立留出尚未验收；若用LLM裁判，还需实际人工/模型校准和明确模型配置、调用授权。设计任务与原神原作表现分别报告。
- **最小GRPO：** 单步动作的框架接入、奖励失败处理与微型更新已验证；还缺真实任务训练输入、适用于本任务的实际人工/模型校准、奖励组合验收及真实模型资源实测。整段多轮轨迹GRPO未实现。Windows应用控制阻止bitsandbytes DLL（WinError4551），当前不能认定4-bit可用；CPU微型更新不受影响。

边界更新：集群策略模型采用ModelScope `Qwen/Qwen3-8B`；仓库裁判示例采用OpenAI官方地址 `https://api.openai.com/v1` 和 `gpt-6-astra`。代码默认不请求API，集群显式启用后发送裁判可见材料；不上传权重或密钥。本地本轮没有调用外部裁判、下载权重或训练真实模型。GPU租赁仍未授权；新运行使用新目录。

2026-09-22 发布授权：用户明确要求将当前代码、测试、审核材料和文档提交并推送到既有 `origin/main`，供集群拉取。`runs/`、`data/raw/`、模型权重和 `.env` 继续留在本地；集群首次拉取不包含本地校准包、运行结果或模型。最近46项单元测试通过；此次仅补充发布记录，不启动训练。

## 当前产物入口

- [集群快速开始](CLUSTER_QUICKSTART.md)、[密钥配置模板](../.env.example)、[API连通性检查](../scripts/check_judge_api.py)。本轮 `runs/cluster_api_20260922_01/`：单进程/双进程微型更新、测试与保护核验；API只用离线传输夹具，不含真实密钥或远程调用结果。
- [GRPO本地接入说明](GRPO_LOCAL.md)、[TRL训练入口](../scripts/train_grpo.py)、[裁判执行/统计入口](../scripts/calibrate_judge.py)。本轮产物 `runs/grpo_20260922_01/`：`tiny_grpo_01/verification.json`、`local_judge_smoke_01/report.json`及`calls/`原始失败、`delivery_report.json`。
- [运行入口](../scripts/run_agent.py)、[任务环境](../characore/agent.py)、[本地推理](../characore/local_policy.py)、[机制测试](../tests/test_agent.py)；启动与参数见[README](../README.md)。
- 本轮本地产物：`runs/agent_20260922_01/`。`mechanics_01/verification.json`记录三种脚本验证，各子目录有`trajectory.json`；`qwen3_1p7b_01/episode/trajectory.json`记录真实失败；`delivery_report.json`记录运行成本、环境与保存检查。
- [来源审核报告](GENSHIN_STAGE_B_DATA_AUDIT.md) 与 [审核决策](stage_b_data_audit_v01/review.json)、[逐项证据](stage_b_data_audit_v01/evidence.json)；导出脚本 scripts/audit_genshin_sources.py。
- 本地当前审核：runs/stage_b_20260922/data_audit_v01_03/，核验 data_audit_v01_03_verification.json；_01失败记录及_02中间结果保留。
- [v0.3人工校准流程](GENSHIN_STAGE_B_CALIBRATION.md)；当前材料 runs/stage_b_20260922/calibration_v03_02/，开发快照 dev_snapshot_v03_01/。人工只读取human材料并复制空白表填写，首轮锁定前不看audit预期。
- [原神初步选型](GENSHIN_PILOT.md)、[CoSER选型](DATA_SELECTION.md) 和 [v0.2协议](GENSHIN_STAGE_B.md) 保留为来源与版本记录，不作为当前实施清单。

## 本轮API与双进程适配

- 策略固定为ModelScope Qwen3-8B；`.env.example`中的裁判配置为OpenAI官方地址`https://api.openai.com/v1`及`gpt-6-astra`。实际账户权限和API调用尚待验证，示例配置不等于校准通过。
- `.env`只做字面配置解析，不执行shell、不展开变量；环境变量优先，密钥不进入日志、元数据或Git。API支持Chat Completions/Responses，不自动附加temperature/reasoning；显式`--allow-api`才发送材料。单请求检查不依赖本地原神缓存，可以直接在集群首次克隆后执行。
- V100按compute capability选择FP16，不能以可能支持软件模拟的`is_bf16_supported()`返回True推断原生BF16。新增`--precision`，保留旧DPO默认行为；Agent和GRPO使用新选择器。
- GRPO按LOCAL_RANK绑定GPU，microbatch降为1，使用梯度累积与完整生成组；API裁判不占本地GPU。结果写入独立rank目录；奖励先跨rank合并校验，任何rank缺失/失败/组内输入不一致则同步中止更新，同分不制造差异。两张卡使用DDP，不等同于单个64GB显存池。
- 实际验证：57项单元测试通过；单进程CPU微型更新2步通过。Windows原生torchrun因PyTorch未构建libuv在rendezvous前失败；改用项目内Gloo/FileStore测试启动器，两进程各2步更新、12个LoRA张量变化、梯度有限非零、冻结参考未变、保存重载误差0；跨rank适配器参数与保存哈希一致。仅CPU机制结果，未验证真实双V100显存/吞吐，也未调用外部裁判API。官方GPT-6 Astra文档已核对模型ID存在，实际账户权限尚待验证。
- 当前可在集群执行更新代码→填`.env`→配置检查/一次API试调用→下载Qwen3-8B→FP16真实基线→CPU双进程检查。任务专属校准、人审和训练输入仍待准备，不将单请求连通性或微型更新冒充校准通过。

## 上一轮GRPO框架接入与实际验证（历史）

- 参考已安装TRL 0.26.2源码，复用GRPOTrainer和PEFT；未手写优势估计、KL、裁剪或优化器。现有Transformers不满足TRL原生tools接口的≥5要求，自定义rollout入口依赖vLLM，因此先实现固定状态下的一步动作GRPO，完整任务仍用已有CLI评测，不冒充整段轨迹训练。
- 新增校准结果汇总：核验请求、原始响应、模型身份、独立人审、锁定首轮与仲裁绑定；统计AB/BA、人工一致率、引用复核、分组/类别/位置/维度分布，缺失保持pending。既有36对、空白人审表及旧包均未修改。
- 本地Qwen3-1.7B从原混排日程试跑`cal_next_history_AB`和`cal_next_insufficient_AB`；各2次真实推理均漏顶层reason，分别生成657/677 token，未触及1024上限。保留原始响应和严格解析失败，不自动修补、不将70个未调用请求当实测失败；不声称完成模型校准。
- 奖励适配区分策略非法动作与裁判故障：前者可为负奖励；技术失败、证据不足、顺序冲突、非有限值中止更新。相同奖励组不制造差异，重复承诺不记状态推进。奖励权重是显式待验收提案，不是已验证的角色奖励。
- 用户本轮明确允许“CPU随机微型模型的GRPO机制测试”；实际2步、每组4样本，12个LoRA张量更新，24次梯度回调中18次非零，全部有限；冻结参考相同，probe logits最大变化0.006885454，保存重载误差0。奖励是合成token-ID均值，不是LLM裁判奖励，不训练Qwen。
- 新增11项测试，全部46项单元测试通过；覆盖真实环境转移、奖励缺失/同分/非法动作、AB/BA、原始响应绑定、人审锁定、范围不迁移、开发集不能改称留出及文件冻结。测试内合成人审仅在临时目录，未写入任何实际人工表。
- 首次微型运行暴露bitsandbytes DLL被系统策略阻止，但非量化CPU测试完成；不绕过Windows控制，不改外部环境。真实GPU GRPO显存、吞吐和4-bit兼容性仍未验证。
- 新输出全部进入`runs/grpo_20260922_01/`；HEAD仍为4a9b48b，无外部API、上传、下载、提交或推送。只在新文件和README/PROGRESS中增补本轮实现，保留上一轮Agent代码与轨迹、审核代码与材料、设计精简和所有旧产物。

## 上一轮Agent实现与实际验证（历史）

- `inspect_clue`读取可发现线索并更新已知事实；`query_status`读取真实状态；`submit_action`校验字段类型、行动名和前置条件后承诺/移动/递交/放弃。隐藏路线内容只有通过工具发现后进入策略输入；世界表不进入提示。封条、位置、承诺轮次、任务状态均为显式字段。
- 每轮事件保存台词、工具、参数和实际返回；策略只接收当前可见状态和既往事件。默认12步，最多允许50步；拒绝与解析失败都耗费步数，终止后拒绝再执行。模型口头宣称不算完成；不运行任意代码工具。
- 按轮分别落盘输入和结果，再汇总JSON轨迹。回放重新执行记录中的输出并校验输入、工具返回、状态变化、终态及统计；不重新推理。原始模型输出、token计数、延迟、显存峰值、模型/源代码哈希均保留，目录独占创建，失败记录不覆盖。
- 软件机制验证：正常路径6轮/6次工具成功；非法路径8轮/6次成功/2次拒绝；承诺路径7轮/6次成功/1次拆封拒绝。承诺在第2轮建立，第5轮拆封尝试被拒绝，随后仍能递交；被拒绝调用造成的任务进度变更为0。脚本结果不是基座表现，也不证明模型具有长期记忆能力。
- 真实运行：本地Qwen3-1.7B revision `70d244cc86ccca08cf5af4e1e306ecf908b1ad5e`，BF16、未量化、greedy、seed=17、关闭thinking、每轮最多192新token、总12轮。第1轮未看委托单即承诺被拒；第2轮查看委托单；第3轮成功承诺；第4至12轮重复承诺，最终`step_limit`，未移动、未递交。12次工具请求中11次成功、1次拒绝，解析失败0、推理异常0；11/12工具成功不等于完成。封条由环境约束保护，不能据此声称角色或记忆能力验证通过。
- 复用现有只读环境和模型缓存；torch `2.5.1+cu121`、transformers `4.57.3`、accelerate `1.12.0`。默认python缺少transformers/accelerate，但已有环境可复用，未安装依赖、未下载权重。准确解释器路径与模型路径在本地交付报告中。
- 验证：35项单元测试通过（原27+新增8），含隐藏线索、前置条件、非法参数、承诺、终止、解析/推理失败、禁止覆盖及回放篡改拒绝；三条脚本轨迹及真实基座轨迹均成功重新执行回放。未运行训练。
- HEAD仍为`4a9b48b`。入场681文件的哈希清单保存于`entry_manifest.json`；除本轮获授权更新的README/PROGRESS外，入场文件须全部保持原字节内容，核验见`delivery_report.json`。未更改设计总纲、审核代码/材料、36对刺激、人工表、原始缓存或旧运行；无外部API、上传、租GPU、提交、推送或远程创建。

启动（从项目根目录执行，使用新输出名）：

~~~powershell
python scripts/run_agent.py verify --output runs/agent_verify_02
python scripts/run_agent.py run --policy scripted --scenario commitment --output runs/agent_demo_02
python scripts/run_agent.py replay runs/agent_20260922_01/qwen3_1p7b_01/episode/trajectory.json
~~~

## 上一轮设计精简与验证（历史）

- 删除DESIGN中的未获数据支持的规模目标、重复阶段计划及扩展性实验要求；近期实现限定为单角色、短任务、三类工具和本地事件记录。
- 删除DATA_SELECTION的容量外推、未实施适配方案与字段映射；删除GENSHIN_PILOT的规模配额和重复实施顺序。README、校准说明与审核报告统一指向当前路线。
- 保留已实现且有实际用途的DPO、来源校验、信息隔离、裁判解析及人工盲标工具；没有为精简而重写代码或放宽训练验收。
- 验证：8份Markdown的30个本地链接可解析，编码与内容检查通过，git diff --check通过；代码、测试、实验配置、来源索引、原始缓存及Stage B历史产物共664文件的路径与内容哈希清单前后一致。仅改文档，未重跑训练或单元测试；没有新增能力验证结论。
- 入场未提交的数据审核代码、测试及JSON决策保留；历史交接记录保留。HEAD仍为4a9b48b，本轮无模型调用、训练、提交或推送。

## 历史交接记录

以下保留当时状态、失败和产物定位；其中旧规模、全池优先和串行门槛不再作为当前待办。当前任务以上文为准。

- 2026-09-21：按中文原神角色优先的方向补查三角色数量、字段与实样，新增 `docs/GENSHIN_PILOT.md`；CoSER 调为备选。明确先基座/GRPO、再 DPO 对照的实施顺序，未扩展为完整 Agent 运行时。只核查公开材料并更新文档，无训练、付费调用、上传、提交或推送。

- 2026-09-21：完成有限现成数据选型，新增 `docs/DATA_SELECTION.md` 与最小适配方案，推荐 CoSER 原始结构化数据；ChatHaruhi 保留为中文 ACG 备选。复用既有材料并下载约 7.26 MiB 公开数据，完成实样与划分核查。未改训练接口、未重跑训练机制测试，未训练、付费调用、上传、提交或推送。保留入场时 README、DESIGN、PROGRESS 的已有修改及 DATA_PLAN 的删除状态。

- 2026-09-21：文档精简为设计总纲与进度交接两份；数据选择要求和输入契约合并进总纲，跨对话入口同步简化。当前阶段仍为数据选型。

- 2026-09-21：建立设计总纲与文档入口；整理独立项目目录；保留 DPO 基础组件，移除不用于新路线的数据、专用评测脚本和结果说明。当前任务是数据选型，不重复无关的微型训练验证。
- 本轮 8 项独立单元测试通过，覆盖显式数据入口、冻结文件篡改、训练文件篡改、情境族隔离、重复输入、提示标签隔离及偏好转换。公共目录扫描未发现旧项目名称、个人机器路径或失效旧脚本引用。文档链接已核对。没有运行新训练、下载模型或对外发布。

后续每轮更新：当前阶段、完成产物及路径、实际验证、未决问题、下一步最小任务。实验结果写独立报告，不把对话日志复制进交接文件；方向改变时同步更新总纲。

- 2026-09-22：完成阶段 B 初版准备，新增 `docs/GENSHIN_STAGE_B.md` 与 `docs/GENSHIN_STAGE_B_CONTEXTS.jsonl`。复用已缓存 ChatHaruhi revision `290bf4ad22076156083804013012847a77c0646c` 的 10 个来源锚点（绫华 4、钟离 3、胡桃 3），整理为 16 个带来源路径、SHA-256、上游行号、可知信息和排除边界的响应切点，其中 12 条暂列 dev_review、4 条 quarantine；按 `family` 计为 10 个来源族，不把同源切点当独立事件。协议固定角色一致性、情境与历史一致性、行动合理性、对话质量四维 0–4 评分，允许行动维度 `NA`，保留 `tie/insufficient`，并加入 5 个校准案例及候选顺序交换要求。已验证 JSONL 16 行/16 个唯一 ID/10 个 family 可解析，必需溯源字段齐全，未调用模型、裁判、训练、付费 API、GPU、上传、提交或推送。
- 2026-09-22：待验证仍包括完整 527 个文本的事件归并、正式 train/dev/test 冻结、实际 30–50 个 dev 比较的裁判校准、解析失败/同分率/位置偏差和 token 预算。已缓存的 347 行生成对话继续仅作待审候选来源，未作为角色证据、chosen 或正确答案。
- 2026-09-22：复跑既有 `python -m unittest discover -s tests -v`，8 项测试全部通过；测试覆盖协议组件，不构成原神数据质量或角色效果验证。

- 2026-09-22 阶段 B v0.2：新增 `scripts/prepare_genshin.py`，完整缓存固定版本三角色 .txt 并校验 Git blob/字节数/SHA-256。实测 528 个文本、526 份不同内容，绫华/钟离/胡桃为117/270/141，正文281,635字节；复用11、补下载517，无失败。此前529/527口径误含绫华 notebook，已更正；语义独立情境数仍未知。缓存 `data/raw/genshin_texts_v1/`；全池报告 `runs/stage_b_20260922/full_pool_01/`。
- 2026-09-22 阶段 B v0.2：新增 `characore/contexts.py` 和 `characore/judge.py`，完成无标签开发快照、train/dev/test 分组/来源/暴露隔离校验、公开历史白名单提示、严格裁判响应解析及 AB/BA 离线导出。修复旁白提前泄露请求的两处输入；实际快照为12个dev截点、9个文件来源族、6个保守剧情组，另4条隔离，train/test为空。快照位于 `runs/stage_b_20260922/dev_snapshot_01/`，明确不可正式训练，不兼作旧DPO套件。
- 2026-09-22 阶段 B v0.2：四维协议和6个完整诊断候选对保存在 `experiments/genshin_stage_b_v02/`，更新 `docs/GENSHIN_STAGE_B.md`。离线导出12个交换顺序请求到 `runs/stage_b_20260922/judge_requests_01/`；实际调用0，预期标签为助手拟写、未经人工裁定。后续计划36个独特比较/72次主调用，含重试最多144次，输入/输出上限589824/147456 token；提供方与模型单价未定，未经预算批准不得外发。
- 2026-09-22 实际验证：17项单元测试通过（原8项+新9项），覆盖缓存篡改、未来台词/隐藏参考隔离、dev哈希、分组/来源/上游留出/开发暴露泄漏、裁判顺序映射、伪造证据、非法分数/NA及技术失败与证据不足分离。首次合成夹具因Windows换行失败，改为字节写入后通过。测试不证明角色能力或裁判可靠性。
- 2026-09-22 待验证：全池528文件仅完成逐文件完整性、精确去重与长句重叠提示；未完成全量语义事件归并、角色稳定偏好逐项证据、正式留出冻结或真实裁判校准。没有新训练、模型调用、外部上传、租赁、付费API、提交或推送。

- 2026-09-22 交付复核：`runs/stage_b_20260922/delivery_verification.json` 实测10个索引来源哈希、快照完整性、12个请求中的参考/预期标签隔离及AB/BA互换均通过；`git diff --check` 无空白错误。
- 2026-09-22 阶段 B v0.3：复核发现v0.2证据ID包含隐藏后续话题，新增中性E编号、候选引用及成对引用校验，拒绝重复JSON键；行动必需时允许真正的证据不足但禁NA。显式保留v0.2请求/解析兼容，原v0.2计划、案例、快照及请求均未覆盖。
- 2026-09-22 阶段 B v0.3：新增 experiments/genshin_stage_b_v03/，6种子+30新比较=36对；仍仅12个dev截点、9个文件来源族、6个保守组，不能计作36个独立情境。新增 characore/calibration.py、docs/GENSHIN_STAGE_B_CALIBRATION.md，明确双人独立盲标、分歧仲裁、分母、技术失败计法和六组统计限制。助手预期不等于人工标签。
- 2026-09-22 实际产物：runs/stage_b_20260922/dev_snapshot_v03_01/、calibration_v03_02/（human空白标注、audit映射与可见证据索引、judge下72个请求）。人工结果0、模型调用0，calibration_passed=false；train/test仍为空，无正式freeze，无训练。稳定偏好逐项验证列表仍为空，未完成全池语义归并或独立留出确认。
- 2026-09-22 实际验证：23项单元测试通过（原17+新增6）；v03_delivery_verification_02.json核验528缓存的Git blob/字节数/SHA-256、526不同内容、旧快照完整性、旧12请求messages逐项重建一致、新72请求AB/BA互换与隐藏元数据隔离、两份空白人工表和新包哈希。测试证明离线软件行为，不证明裁判可靠性或角色效果。未调用外部API、上传数据、租GPU、提交、推送或创建远程。

最终离线包为 calibration_v03_02；_01 保留作过程产物，候选引用范围的提示措辞已在最终包统一。两包均无模型或人工结果。

- 2026-09-22 阶段B数据审核v0.1：入场Git干净，HEAD为`4a9b48b`、前一提交`b5af38b`，无适用AGENTS.md。新增`docs/stage_b_data_audit_v01/review.json`及`evidence.json`、审核报告和离线脚本。528文本中313份归入9链/8块，196语音/自述、14未解决片段、1信件片段、4人物/设定材料；另核验3份system_prompt，共531条用途账本。链数不是独立事件数。
- 2026-09-22 实际发现：绫华委托、胡桃两条主剧情、钟离送仙和盐神链继承开发暴露；矿工与赠扇因老石/若陀回述共块，品茶另块。只有3链60文件/2块作为数据审核储备，抽出6个原文截点但未分配train/test。长prompt跨事件概述另记包含关系；跨角色共享模板不当成剧情，眼狩令与往生堂历史另审。修正两处文件名前缀误判，并隔离生日对象、胡桃所在地等包装错误。
- 2026-09-22 证据进展：20项逐条来源定位与时间限制，其中18项有范围限制的源文支持、2项拒绝泛化；未生成已验收profile，未把隐藏后续或目标回答放入策略。证据设计来源记录开发用途，储备仅作数据审核，不写候选/偏好。旧快照、旧请求和校准包保持原样。
- 2026-09-22 本地产物：`runs/stage_b_20260922/data_audit_v01_03/`为当前版本；`_01/failure.json`记录去除说话人后阈值漏掉5对旧重叠的失败，`_02`为保留的中间产物。最终104对正文包含候选覆盖旧61对，新增43对；3对跨角色命中均为system_prompt模板。机器无命中不证明语义独立。
- 2026-09-22 验证：新增4项来源审核单元测试通过；实际528文本与3份system_prompt的Git blob/大小/SHA-256、313个成员定位、20项证据引用、6截点历史早于目标的检查通过。交付核验见`runs/stage_b_20260922/data_audit_v01_03_verification.json`；`git diff --check`通过。未重跑训练测试；这些验证不是角色效果或裁判可靠性验证。
- 2026-09-22 未决阻塞：正式train/test仍0/0，储备事件独立性/时间证据/历史用途和缩小后的规模未验收；36对人工仍0、模型调用仍0、calibration_passed=false；裁判提供方/模型/tokenizer/价格/调用授权未定。基座评测需先完成数据冻结与真实校准；最小GRPO还需奖励组合和失败处理验收、采样评分更新适配及6GB显存/吞吐实测。本轮无外部API、上传、租GPU、下载模型、训练、提交、推送或远程创建。
